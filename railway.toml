# Railway Configuration
# Este archivo le indica a Railway cómo construir y desplegar la aplicación

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

# Variables de entorno para el BUILD (se inyectan durante npm run build)
[build.buildArgs]
VITE_API_URL = "https://carwashcloudapi-production.up.railway.app"

[deploy]
# Railway usa la variable $PORT automáticamente
# serve escuchará en el puerto que Railway asigne
startCommand = "serve -s dist -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
